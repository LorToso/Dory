language: java
cache: true
jdk:
    - oraclejdk8

before_install:
    - sudo apt-get update
    - sudo apt-get install -u lib32stdc++6 lib32z1
    - wget http://dl.google.com/android/android-sdk_r23.0.2-linux.tgz
    - tar -zxf android-sdk_r23.0.2-linux.tgz
    - setenv ANDROID_HOME android-sdk_r23.0.2-linux

    - sudo chmod 777 gradlew
    - mkdir ~/.android

install:
    - sudo ./gradlew clean assemble
before_script:
    - mkdir -p shippable/testresults
script:
    - ./gradlew check
after_script:
    - cp -r build/test-results/* shippable/testresults/
notifications:
    email:
        recipients:
            - lortoso@gmail.com